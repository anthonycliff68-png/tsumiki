import { Welcome } from '@/components/Welcome';

/** Temporary: look at the welcome carousel before it is wired into the flow. */
export default function WelcomePreview() {
  return <Welcome onStart={() => {}} onInvite={() => {}} />;
}
